RSpec.describe FmData::Spyke::Relation do
  describe "#initialize" do
    xit "sets the default limit if any"
  end

  describe "#limit" do
    xit "creates a new scope with the given limit"
  end

  describe "#offset" do
    xit "creates a new scope with the given offset"
  end

  describe "#sort" do
    xit "creates a new scope with the given sort params"
  end

  describe "#portal" do
    xit "creates a new scope with the given portal params"
    xit "accepts a list of arguments, a single string or an array"
  end

  describe "#query" do
    xit "creates a new scope with the given query params merged with previous ones"
  end
end
